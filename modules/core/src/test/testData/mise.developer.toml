# Environment-specific tasks for developer environment
[tasks]
"dev-task" = { run = 'echo "Developer environment task"', description = "Task only in developer environment" }

[tasks.dev-build]
run = '''
#!/usr/bin/env bash
echo "Building for developer"
'''
description = "Developer build task"
