[project]
name = "issue514-django"
version = "0.1.0"
description = "Django reproduction project for intellij-mise issue 514"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2,<7.0",
]
